const sum = (x, y) => {
  //   console.log(x + y);
  return x + y;
};
const sub = (x, y) => {
  console.log(x - y);
  // return;
};
const mul = (x, y) => {
  console.log(x * y);
  // return;
};
const div = (x, y) => {
  console.log(x / y);
  // return;
};

module.exports = { sum, sub, mul, div };
